# NL Lanka Travel & Tours (demo)

    npm install
    npm run dev      # local preview
    npm run build    # production build in /dist

Deploy on Vercel: import the repo, framework "Vite", build `npm run build`, output `dist`.
